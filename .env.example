# 服务端口（内部端口，Nginx 反代到 80/443）
PORT=3001

# JWT 密钥 — 生产环境务必改成随机长字符串
# 可用 `openssl rand -hex 32` 生成
JWT_SECRET=change-me-to-a-random-secret

# 管理员账号 — 首次启动时自动创建
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-please

# 数据持久化目录（绝对路径）
DATA_DIR=/var/lib/nimbus-chat
